name: Action Branch Check

on:
  pull_request:
    branches: [stage]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/stage_test'
    steps:
      - name: Deploy app 
        run: exit 1
