name: Action Branch Check

on:
  pull_request:
    branches:
      - master
      - stage
      - dev

jobs:
  branch_check:
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/stage_test'
    steps:
      - name: branch check app 
        run: echo ${github.ref} 
        run: echo "It's not available branch. (dev->stage->master)"
        run: exit 1
        #  build:
        #    need: branch_check
