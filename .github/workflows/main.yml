name: Branch Check

on:
  pull_request:
    branches:
      - master
      - stage
      - dev

jobs:
  master_branch_check:
    if: github.event_name == 'pull_request' && github.base_ref == 'master'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: master check branches
        if: github.head_ref != 'stage'
        run: echo "It's not available branch. (stage->master)" && exit 1
      - name: success
        if: ${{ success() }}
        run: exit 0
  stage_branch_check:
    if: github.event_name == 'pull_request' && github.base_ref == 'stage'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: stage check branches
        if: github.head_ref != 'dev'
        run: echo "It's not available branch. (dev->stage)" && exit 1
      - name: success
        if: ${{ success() }}
        run: exit 0
